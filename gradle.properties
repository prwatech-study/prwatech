# Gradle 7.6.1 cannot run on JDK 21 (this machine's default). The project targets
# Java 17 (sourceCompatibility), so pin the build to a JDK 17 install.
org.gradle.java.home=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
