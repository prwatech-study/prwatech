package com.prwatech.skillama.model;

/** Corporate role within an organization. Platform staff use {@code organizationId=null} and {@code orgRole=null}. */
public enum OrgRole {
    ORG_OWNER,
    ORG_ADMIN,
    MANAGER,
    LEARNER
}
