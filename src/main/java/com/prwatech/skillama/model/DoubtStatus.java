package com.prwatech.skillama.model;

/** Lifecycle status of an AI Mentor doubt. Label-only for MVP — no live mentor routing. */
public enum DoubtStatus {
    PENDING,
    SOLVED,
    NEEDS_MENTOR,
    RESOLVED
}
